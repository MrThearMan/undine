---
description: Use when running Python commands, tests, linting, or to set up a virtualenv
alwaysApply: true
---

Use Poetry for all Python execution. Never run `python` or `pytest` directly.

```bash
poetry install --all-extras --all-groups
poetry run pytest ...
poetry run python manage.py ...
```
