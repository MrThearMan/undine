name: Release

on:
  release:
    types:
      - released

jobs:
  release:
    uses: MrThearMan/CI/.github/workflows/release.yml@v0.6.1
    with:
      uv-cache-suffix: "release"
    secrets:
      pypi-token: ${{ secrets.PYPI_API_TOKEN }}
